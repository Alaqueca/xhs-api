{
  "name": "xhs-api",
  "version": "1.0.0",
  "description": "小红书笔记数据抓取 API（Vercel）",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}